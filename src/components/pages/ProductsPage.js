import React from "react";
import Description from "../Description";
const ProductsPage = ({ lang, sideBarOpen, parallaxStyle }) => {
  return (
    <div>
      <Description lang={lang} />{" "}
    </div>
  );
};

export default ProductsPage;
