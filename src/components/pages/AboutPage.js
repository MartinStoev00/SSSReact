import React from "react";
import Boosting from "../Boosting";

const AboutPage = ({ lang, sideBarOpen, parallaxStyle }) => {
  return (
    <div>
      <Boosting lang={lang} />
    </div>
  );
};

export default AboutPage;
